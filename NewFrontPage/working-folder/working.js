var mWidth = '35';

	function changeWidth () {
		//callout
		var callOut = document.querySelector('.img-callout');
		
		callOut.style.max-width = imageWidth *

		//image container
		var image = document.querySelector('.img-bck');

		//width of image container
		var imageWidth = image.offsetWidth;


		
		// increase max width as a percentage of the width of the image container
	}

	// var left = 0; 
	// function frame() {
  //   var element = document.querySelector('.item-relative');
  //   left += 2;
  //   element.style.left = left + 'px';
  //   if (left >= 300) {
  //     clearInterval(id)
  //   }
  // }

  // var id = setInterval(frame, 10)
